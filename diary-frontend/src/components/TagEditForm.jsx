// TODO: добавить PATCH /tags/{id} на бэкенде
// Пока недоступно — эндпоинт для изменения тега ещё не реализован

export default function TagEditForm({ tag, onUpdated }) {
    return (
        <div>
            <p>Изменение тега будет доступно позже</p>
        </div>
    )
}
